// Per-run orchestrator. Stubbed out in Task 1 so src/index.js has something
// real to import; Task 13 replaces this with the full clone -> pre-filter ->
// check -> post -> poll pipeline described in the plan.
export async function run(opts) {
  return { exitCode: 0, stats: { note: 'run not implemented' } };
}
